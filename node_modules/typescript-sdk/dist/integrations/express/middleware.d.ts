import Keploy from "../../src/keploy";
import { Request, Response, NextFunction } from "express";
import { StrArr } from "../../proto/services/StrArr";
export declare function getRequestHeader(headers: any): {
    [key: string]: StrArr;
};
export declare function getResponseHeader(header: any): {
    [key: string]: StrArr;
};
export default function middleware(keploy: Keploy): (req: Request, res: Response, next: NextFunction) => void;
export declare function afterMiddleware(keploy: Keploy, req: Request, res: Response): void;
