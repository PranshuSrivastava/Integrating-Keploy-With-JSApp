export declare const MODE_RECORD = "record", MODE_TEST = "test", MODE_OFF = "off";
export default class Mode {
    mode: string;
    constructor();
    static Valid(m: string): boolean;
    GetMode(): string;
    SetTestMode(): void;
    SetMode(m: string): Error | undefined;
    GetModeFromContext(): any;
}
